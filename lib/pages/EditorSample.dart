import 'package:flutter/material.dart';

import 'editor/editor.dart';

class EditorSample extends StatelessWidget {
  EditorSample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Editor();
  }
}
