import 'package:fluent_ui/fluent_ui.dart';

class CertPage extends StatelessWidget {
  const CertPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('View Cert'),
      ),
    );
  }
}
